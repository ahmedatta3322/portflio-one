<h1>New portoflio item</h1>
<%= form_for(@port_items) do |form| %>
<% if @port_items.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@port_items.errors.count, "error") %> prohibited
      this article from being saved:
    </h2>
    <ul>
      <% @port_items.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_area :subtitle %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
